.user-name
  h2
    = @user.name

  - if @user.id == current_user.id
    .pull-right
      = link_to 'WebCal feed', user_path(@user, format: :ics, only_path: false, protocol: :http)

  h4 Registered events
  = render @user.registered_events
